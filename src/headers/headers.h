# ifndef HEADERS_H

    # define HEADERS_H

    # include <stdio.h>
    # include <string.h>
    # include <stdlib.h>
    # include <ctype.h>
    # include <stdint.h>

    # define true                ( 1 )
    # define false               ( 0 )

    typedef int8_t byte;
    typedef uint8_t bool;
    typedef char *string;

# endif
